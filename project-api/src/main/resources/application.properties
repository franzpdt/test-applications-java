spring.application.name=project-api
server.port=${APP_PORT:5000}
server.tomcat.threads.max=20

# Log directory — picked up by logback-spring.xml; override with APP_LOG_PATH env var
logging.file.path=${APP_LOG_PATH:./logs}

